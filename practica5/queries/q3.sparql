PREFIX ex: <http://example.org/europe#>

SELECT ?country ?name
WHERE {
    ?country a ex:Country ;
    ex:countryName ?name .
    FILTER(STRSTARTS(?name, "A") && STRENDS(?name, "a"))
}
ORDER BY ?name
